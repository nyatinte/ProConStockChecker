# ProConStockChecker

# SetUp

1. 仮想環境を構築
`python -m venv .venv`

2. 仮想環境を有効に
`source .venv/bin/activate`

3. パッケージのインストール
`pip install -r requirements.txt`

4. .envファイルにトークンの記述
ex) `$ echo "YOUR_NOTIFY_TOKEN=hogehoge" > .env`
